import 'package:flutter/material.dart';

class LeadersPage extends StatelessWidget {
  final String title;

  const LeadersPage({Key? key, required this.title}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
